{
  "name": "qtc-runner",
  "version": "1.0.0",
  "description": "Local agent that picks up Test_Run__c jobs from Salesforce and executes Playwright tests",
  "main": "agent.js",
  "scripts": {
    "start": "node agent.js",
    "start:debug": "node --inspect agent.js"
  },
  "dependencies": {
    "node-fetch": "^3.3.2",
    "form-data": "^4.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
